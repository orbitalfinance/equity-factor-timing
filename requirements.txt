# Equity Factor Timing - Python dependencies
# Pinned to the versions used to produce the committed results (Python 3.12).
# Install with:  pip install -r requirements.txt

# --- Core data stack ---
numpy==2.0.0
pandas==2.2.2
scipy==1.13.1
openpyxl==3.1.4          # read/write the .xlsx market-data files

# --- Machine learning ---
scikit-learn==1.5.0
imbalanced-learn==0.12.3  # SMOTE oversampling
hyperopt==0.2.7           # Bayesian hyper-parameter tuning (stacking model)
statsmodels==0.14.2       # Augmented Dickey-Fuller stationarity test
joblib==1.4.2             # (de)serialize the trained .pkl models

# --- Portfolio construction ---
Riskfolio-Lib==6.2.3      # risk-parity allocation

# --- Plotting & reporting ---
matplotlib==3.9.0
seaborn==0.13.2
tabulate==0.9.0

# --- Notebook runtime ---
jupyterlab>=4.0,<5.0
ipykernel==6.29.4
